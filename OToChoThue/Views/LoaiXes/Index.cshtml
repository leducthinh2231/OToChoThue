@model IEnumerable<OToChoThue.Models.LoaiXe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenXe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BienSo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaThue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenXe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BienSo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaThue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.XeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.XeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.XeID })
        </td>
    </tr>
}

</table>
